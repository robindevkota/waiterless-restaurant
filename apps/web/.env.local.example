# MUST stay relative — Next rewrites proxy /api to :5000. An absolute URL
# breaks the SameSite refresh cookie and logs users out on reload.
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
